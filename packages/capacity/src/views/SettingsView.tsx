// 設定（メンバー管理）ビュー（ステップ2で実装）。
export function SettingsView() {
  return (
    <div className="main">
      <div className="card">
        <div className="empty-msg">メンバー設定は準備中です（次のステップで移植）。</div>
      </div>
    </div>
  );
}
