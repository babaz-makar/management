# dependencies
node_modules/
.pnpm-store/

# builds
.next/
dist/
out/
build/
*.tsbuildinfo

# turbo
.turbo/

# env
.env
.env.local
.env*.local

# token store (contains OAuth refresh tokens)
# 先頭以外のスラッシュを含むパターンはルート起点になるため、
# apps/web/data/tokens.json も確実に無視されるよう ** を付ける
**/data/tokens.json

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
.vscode/
.idea/

# claude code (personal settings; 共有は .claude/settings.json)
.claude/settings.local.json

# vercel cli
.vercel/

.vercel

# coverage (generated)
coverage/
**/coverage/
